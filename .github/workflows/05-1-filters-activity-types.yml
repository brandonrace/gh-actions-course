name: 05 - 1 - Event Filters and Activity Types

on: 
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: echo "Running whenever PR is opened or synchonized AND base branch is main"
      
