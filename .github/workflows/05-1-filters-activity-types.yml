name: 05 - 1 - Event Filters and Activity Types

on: 
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: "Running whenever PR is opened or synchonized AND base branch is main"
      
